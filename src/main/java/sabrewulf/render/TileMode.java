package sabrewulf.render;

/**
 * This enum was originally used to specify the tiling mode for the sprite.
 * However this enum is now deprecated, and most functionality has been moved
 * to {@link Sprite}.
 */

public enum TileMode {
    PLATFORM,
    WALL,
    SINGLE;
}
